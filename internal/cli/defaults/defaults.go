package defaults

const (
	// RegistryURL is the default endpoint for registry operations (search, install, publish, update).
	RegistryURL = "https://registry.omdr.dev"

	// CLIURL is the default endpoint for user operations (auth, billing, credits, usage).
	CLIURL = "https://cli.omdr.dev"

	// DocsURL is the developer documentation site.
	DocsURL = "https://docs.omdr.dev"

	// WebsiteURL is the main Open MCP Directory website.
	WebsiteURL = "https://openmcpdirectory.com"
)
